{
    "version": "2.0.0",
    "tasks": [
        {
            "command": "npx tsc -p ${cwd}${pathSeparator}discord.ipynb${pathSeparator}",
            "type": "shell",
            "label": "compile_ts"
        },

        {
            "command": "node ${cwd}${pathSeparator}discord.ipynb${pathSeparator}build/index.js",
            "label": "launch",
            "type": "shell",
            "dependsOn": [ "compile_ts" ]
        }
    ]
}